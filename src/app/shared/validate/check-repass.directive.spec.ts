import { CheckRepassDirective } from './check-repass.directive';

describe('CheckRepassDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckRepassDirective();
    expect(directive).toBeTruthy();
  });
});
