export enum StorageConstant {
  accessToken = 'access_token',
  refreshToken = 'refreshToken',
  avatar = 'avatar',
  username = 'username',
  isLogin = 'isLogin',
}

export enum TableStorage {
  defaultPageSizeValue = '30',
  currentPageSize = 'currentPageSize',
  currentSearchParam = 'currentSearchParam',
}
